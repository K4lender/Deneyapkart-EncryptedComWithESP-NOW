# Deneyapkart-EncryptedComWithESP-NOW

Encrypted Communication over ESP-NOW with Deneyap Kart. You must use "base64_encode"  (dojyorin/arduino_base64) library on Arduino IDE for base64 encoding/decoding.
